'use strict';

/**
 * user-resume-builder router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-resume-builder.user-resume-builder');
