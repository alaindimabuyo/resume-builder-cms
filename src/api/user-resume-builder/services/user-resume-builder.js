'use strict';

/**
 * user-resume-builder service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-resume-builder.user-resume-builder');
